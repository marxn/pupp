def g = 10;

def test2 = (&x : int)
{
        transaction(x)
        {
                x=11;
                commit;
        }
};

def main = (args : set) : int
{
        print g,NL;
        call test2(g);
        print g,NL;
        return 0;
};

