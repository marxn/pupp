def main = (args : set) using(&sss): int
{
        def xxx = (a : string) : int
        {
                print a,NL;
                return 0;
        };

        if(true)
        {
                sss="abcd";
                print xxx(sss),NL;
        }

        return 11;
};

def sss : string;

