def f = (x : int ) using(&f): int
{
        print x,NL;
        if(x==1)
        {
                return 1;
        }
        return x*f(x-1);
};

def main = (args : set) using(f): int
{
	print f(5),NL;
	return 0;
};

