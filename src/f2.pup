def main = (args as collection) as integer
{
        def fun_array as function[10];
        def i = 0;

        for(i = 0; i < 10; i = i + 1)
        {
                fun_array[i] = () as null { print i,NL; };
        }

        call fun_array[1]();
        return 0;
};

