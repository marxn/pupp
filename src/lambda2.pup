def lambda_test = (a& as collection, b as function) {
        def i = 0;
        foreach <key,value> in a
        {
                call b(value);
        }
};

def main = (args as collection) as integer {
        def t = collection{1,2,3,4,5,6,7,8,9,0};
        def p = 10;

        call lambda_test(t, (x as integer) {print x + p,NL;});

        return 0;
};

