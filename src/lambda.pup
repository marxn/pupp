def buildcounter = () as function
{
        def x = 0;
        return () as integer {x = x + 1; return x;};
};

def main = (args as collection) as integer
{
        def i as integer;
        def counter as function;

        counter = buildcounter();

for(i=0;i<1000000;i=i+1)
{
        print counter(),NL;
}
        return 0;
};

